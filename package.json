{
  "name": "lightlistv2",
  "version": "1.0.0",
  "description": "タスク管理アプリ",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "setup": "docker-compose up -d && npm run setup --workspaces",
    "dev": "turbo dev",
    "test": "turbo test",
    "build": "turbo build",
    "check": "turbo check",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "@turbo/gen": "^1.10.12",
    "turbo": "^1.10.12"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
